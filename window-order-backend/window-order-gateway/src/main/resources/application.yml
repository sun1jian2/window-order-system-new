server:
  port: 18080

spring:
  config:
    import: "optional:nacos:window-order-gateway.yaml"
  application:
    name: window-order-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 10.100.106.134:8848
        namespace: dev123456
      config:
        server-addr: 10.100.106.134:8848
        namespace: dev123456
        file-extension: yaml
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: auth-service
          uri: lb://window-order-auth
          predicates:
            - Path=/api/auth/**, /api/roles/**, /api/logs/**
        - id: brand-service
          uri: lb://window-order-brand
          predicates:
            - Path=/api/brand/**
        - id: customer-service
          uri: lb://window-order-customer
          predicates:
            - Path=/api/customer/**
        - id: order-service
          uri: lb://window-order-order
          predicates:
            - Path=/api/orders/**, /api/remeasure/**, /api/after-sales/**, /api/sales-target/**, /api/dashboard/**, /api/export-task/**
        - id: file-service
          uri: lb://window-order-order
          predicates:
            - Path=/api/file/**
